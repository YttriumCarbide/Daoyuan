[project]
name = "daoyuan-images"
version = "0.1.0"
description = "Build images.json from data/character/, data/sect/, data/themes/ TOML sources"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.12,<3",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pre-commit>=4.3,<5",
    "tombi==1.1.0",
]
