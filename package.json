{
  "name": "daoyuan-images",
  "version": "0.1.0",
  "description": "Build and consume Daoyuan image data from TOML sources",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./types": {
      "types": "./dist/schema.d.ts"
    }
  },
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsx src/cli.ts",
    "build:check": "tsx src/cli.ts --check",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dist": "tsc -p tsconfig.build.json",
    "format:toml": "tombi format --offline",
    "prepare": "npm run dist"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@tombi-toml/tombi": "^1.3.0",
    "@types/node": "^24.0.0",
    "ajv": "^8.17.1",
    "smol-toml": "^1.3.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0",
    "zod-to-json-schema": "^3.24.1"
  }
}
