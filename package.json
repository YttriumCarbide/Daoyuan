{
  "name": "daoyuan-images",
  "version": "0.1.0",
  "description": "Build and consume Daoyuan image data from TOML sources",
  "type": "module",
  "sideEffects": false,
  "license": "MIT",
  "files": [
    "dist/sdk"
  ],
  "exports": {
    ".": {
      "types": "./dist/sdk/index.d.ts",
      "import": "./dist/sdk/index.js"
    },
    "./types": {
      "types": "./dist/sdk/types.d.ts",
      "import": "./dist/sdk/types.js"
    }
  },
  "types": "./dist/sdk/index.d.ts",
  "main": "./dist/sdk/index.js",
  "engines": {
    "node": ">=20.19.0"
  },
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "build": "tsx src/cli/index.ts",
    "build:check": "tsx src/cli/index.ts --check",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dist": "tsc -p tsconfig.build.json",
    "dist:sdk": "tsc -p tsconfig.sdk.json",
    "format:toml": "tombi format --offline",
    "prepare": "husky && tsc -p tsconfig.sdk.json"
  },
  "lint-staged": {
    "data/**/*.toml": "tombi format --offline --quiet"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@tombi-toml/tombi": "^1.3.0",
    "@types/node": "^24.0.0",
    "ajv": "^8.17.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "smol-toml": "^1.3.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
